[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install && pnpm turbo run build --filter=web"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
